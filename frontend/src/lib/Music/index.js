import Tab from '../Tab.js';

export default class Music extends Tab {
    constructor(parent, options) {
        super(parent, options);
        this.label = 'MUSIC'
        this.tab = 'music';
    }

}
